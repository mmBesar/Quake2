name: Build and Release Quake2 Binaries

on:
  push:
    branches: [upstream]
    tags: [QUAKE2_*]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-release:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - arch: amd64
            platform: linux/amd64
            output: output-amd64
          - arch: arm64
            platform: linux/arm64/v8
            output: output-arm64

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          ref: upstream
          fetch-depth: 0

      - name: Set TAG from Git
        run: |
          echo "TAG=$(git tag --sort=-creatordate | grep QUAKE2_ | head -1)" >> $GITHUB_ENV

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: all

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Quake2 Binaries for ${{ matrix.arch }}
        run: |
          mkdir -p ${{ matrix.output }}

          cat > Dockerfile.build << 'EOF'
          FROM ubuntu:22.04
          RUN apt-get update && \
              DEBIAN_FRONTEND=noninteractive apt-get install -y \
              build-essential cmake pkg-config zip \
              libsdl2-dev libsdl2-image-dev \
              libopenal-dev libcurl4-openssl-dev \
              libx11-dev libxi-dev libgl1-mesa-dev zlib1g-dev && \
              rm -rf /var/lib/apt/lists/*
          WORKDIR /src
          COPY . .
          RUN test -f CMakeLists.txt || (echo "Missing CMakeLists.txt!" && false)
          RUN mkdir build && cd build && \
              cmake .. -DCMAKE_BUILD_TYPE=RelWithDebInfo -DRUNTIME_OUTPUT_DIRECTORY=release && \
              make -j$(nproc)
          RUN mkdir -p /out && \
              cp build/release/quake2 build/release/q2ded /out/ && \
              cp build/release/ref_*.so /out/ || true && \
              mkdir -p /out/baseq2 && \
              cp build/release/baseq2/game.so /out/baseq2/ || true
          EOF

          docker buildx build \
            --platform ${{ matrix.platform }} \
            --file Dockerfile.build \
            --output type=local,dest=${{ matrix.output }} \
            .

      - name: Package Debug ZIP
        run: |
          OUT=Quake2-linux-${{ matrix.arch }}-debug
          mkdir -p $OUT/baseq2
          cp ${{ matrix.output }}/quake2         $OUT/
          cp ${{ matrix.output }}/q2ded          $OUT/
          cp ${{ matrix.output }}/ref_*.so       $OUT/ || true
          cp ${{ matrix.output }}/baseq2/game.so $OUT/baseq2/ || true
          zip -r quake2-linux-${{ matrix.arch }}-${{ env.TAG }}-debug.zip $OUT

      - name: Strip Binaries & Package Release ZIP
        run: |
          OUT=Quake2-linux-${{ matrix.arch }}
          mkdir -p $OUT/baseq2
          cp ${{ matrix.output }}/quake2         $OUT/
          cp ${{ matrix.output }}/q2ded          $OUT/
          cp ${{ matrix.output }}/ref_*.so       $OUT/ || true
          cp ${{ matrix.output }}/baseq2/game.so $OUT/baseq2/ || true

          # Strip binaries
          docker run --rm --platform ${{ matrix.platform }} \
            -v $(pwd)/$OUT:/workspace ubuntu:22.04 \
            bash -c "apt-get update && apt-get install -y binutils && \
                     strip /workspace/quake2 /workspace/q2ded /workspace/*.so 2>/dev/null || true"

          zip -r quake2-linux-${{ matrix.arch }}-${{ env.TAG }}.zip $OUT

      - name: Upload ZIPs to Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG }}
          name: ${{ env.TAG }}
          files: |
            quake2-linux-${{ matrix.arch }}-${{ env.TAG }}.zip
            quake2-linux-${{ matrix.arch }}-${{ env.TAG }}-debug.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
