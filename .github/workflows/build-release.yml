name: Build and Release Binaries

on:
  push:
    branches: [ upstream ]
    tags:    [ 'QUAKE2_*' ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-release:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - arch: x86_64
            suffix: linux-amd64
            cc: gcc
            cxx: g++
          - arch: aarch64
            suffix: linux-arm64
            cc: aarch64-linux-gnu-gcc
            cxx: aarch64-linux-gnu-g++

    steps:
      - name: Checkout upstream
        uses: actions/checkout@v4
        with:
          ref: upstream
          fetch-depth: 0

      - name: Determine TAG
        id: tag
        run: |
          TAG=$(git tag --sort=-creatordate | grep QUAKE2_ | head -1)
          echo "TAG=$TAG" >> $GITHUB_ENV

      - name: Remap ARM64 sources to jammy
        if: matrix.arch == 'aarch64'
        run: |
          # Runner is Ubuntu 24.04 ("noble") but its arm64 security repos are missing.
          sudo sed -i 's/noble/jammy/g' /etc/apt/sources.list

      - name: Install build dependencies
        run: |
          sudo apt-get update
          DEBIAN_FRONTEND=noninteractive sudo apt-get install -y \
            build-essential cmake pkg-config zip \
            libsdl2-dev libsdl2-image-dev \
            libopenal-dev libcurl4-openssl-dev \
            libx11-dev libxi-dev libgl1-mesa-dev zlib1g-dev

          if [ "${{ matrix.arch }}" = "aarch64" ]; then
            sudo dpkg --add-architecture arm64
            sudo apt-get update
            DEBIAN_FRONTEND=noninteractive sudo apt-get install -y \
              gcc-aarch64-linux-gnu g++-aarch64-linux-gnu \
              libsdl2-dev:arm64 libsdl2-image-dev:arm64 \
              libopenal-dev:arm64 libcurl4-openssl-dev:arm64 \
              libx11-dev:arm64 libxi-dev:arm64 \
              libgl1-mesa-dev:arm64 zlib1g-dev:arm64
          fi

      - name: Build with debug symbols
        run: |
          mkdir build && cd build
          CC=${{ matrix.cc }} CXX=${{ matrix.cxx }} \
          cmake .. \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DRUNTIME_OUTPUT_DIRECTORY=release \
            $( [ "${{ matrix.arch }}" = "aarch64" ] && echo "-DCMAKE_SYSTEM_NAME=Linux -DCMAKE_SYSTEM_PROCESSOR=arm64" )
          make -j$(nproc)

      - name: Package Debug ZIP
        run: |
          OUT=Quake2-${{ matrix.suffix }}-debug
          mkdir -p $OUT/baseq2
          cp build/release/quake2         $OUT/
          cp build/release/q2ded          $OUT/
          cp build/release/baseq2/game.so $OUT/baseq2/ || true
          cp build/release/ref_*.so       $OUT/     || true
          zip -r quake2-${{ matrix.suffix }}-${{ env.TAG }}-debug.zip $OUT

      - name: Strip & Package Release ZIP
        run: |
          OUT=Quake2-${{ matrix.suffix }}
          mkdir -p $OUT/baseq2
          cp build/release/quake2         $OUT/
          cp build/release/q2ded          $OUT/
          cp build/release/baseq2/game.so $OUT/baseq2/ || true
          cp build/release/ref_*.so       $OUT/     || true
          strip $OUT/quake2 || true
          strip $OUT/q2ded  || true
          strip $OUT/*.so   || true
          zip -r quake2-${{ matrix.suffix }}-${{ env.TAG }}.zip $OUT

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG }}
          name:     ${{ env.TAG }}
          files: |
            quake2-${{ matrix.suffix }}-${{ env.TAG }}.zip
            quake2-${{ matrix.suffix }}-${{ env.TAG }}-debug.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
